# Configurações do Banco de Dados
# O Railway fornece estas variáveis automaticamente
# Não defina DATABASE_URL aqui, o Railway irá substituir automaticamente
# DATABASE_URL=
# DATABASE_PUBLIC_URL=
# PGDATA=

# Configurações de Segurança
SECRET_KEY=${SECRET_KEY}
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Configurações da Aplicação
APP_NAME=PDV System Backend
DEBUG=False
ENVIRONMENT=production

# Configurações de CORS
# Atualize com o domínio do seu frontend em produção
ALLOWED_ORIGINS=["https://seu-frontend-dominio.com", "https://www.seu-frontend-dominio.com"]

# Configurações de Email (futuro)
SMTP_HOST=${SMTP_HOST}
SMTP_PORT=${SMTP_PORT}
SMTP_USER=${SMTP_USER}
SMTP_PASSWORD=${SMTP_PASSWORD}