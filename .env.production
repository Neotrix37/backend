# Configurações do Banco de Dados
# O Railway fornecerá a variável DATABASE_URL automaticamente

# Configurações de Segurança
SECRET_KEY=${SECRET_KEY}
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Configurações da Aplicação
APP_NAME=PDV System Backend
DEBUG=False
ENVIRONMENT=production

# Configurações de CORS
# Atualize com o domínio do seu frontend em produção
ALLOWED_ORIGINS=["https://seu-frontend-dominio.com", "https://www.seu-frontend-dominio.com"]

# Configurações de Email (futuro)
SMTP_HOST=${SMTP_HOST}
SMTP_PORT=${SMTP_PORT}
SMTP_USER=${SMTP_USER}
SMTP_PASSWORD=${SMTP_PASSWORD}