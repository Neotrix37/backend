{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python -c 'import os; from app.core.config import settings; print(\"Database URL:\", settings.DATABASE_URL)' && alembic upgrade head && gunicorn -c gunicorn_config.py main:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
